class Player {
  int id;
  String name;
  int sum;
  String outCards;
  String inCards;
  String uselessCards;

  Player(
      {required this.id,
      required this.name,
      required this.sum,
      required this.outCards,
      required this.inCards,
      required this.uselessCards});
}
